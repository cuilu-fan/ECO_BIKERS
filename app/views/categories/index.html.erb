<div class="wraper">
  <p class="category-text">Discover our reloved <span class="Spantext">bikes</span> bellow!</p>
  <div class="blob-grid">
    <% @categories.each do |category| %>
    <%= link_to bikes_path do %>
      <div class="blob">
        <%= category.name %>
      </div>
      <% end %>
    <% end %>
  </div>
  <p class="category-text-Bl">Contact us: <span class="Spantext">contact@ecoobikes.com</span></p>
</div>
