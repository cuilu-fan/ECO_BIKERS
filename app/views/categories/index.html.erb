<div class="wraper">

  <div class="blob-grid">

    <% @categories.each do |category| %>
      <div class="blob">
        <%= link_to category.name, bikes_path %>
      </div>
    <% end %>
  </div>
</div>
